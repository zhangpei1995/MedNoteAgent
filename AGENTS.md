# Agent Instructions

本项目处于重新实现阶段。执行任务时以当前目标、验收标准和本文件规则为准，交付正式工程链路中的高质量代码，不保留未被需求明确要求的旧入口、旧行为、兼容层、临时 demo 或割裂实现。

## P0 工作原则

- 先判断当前任务要解决什么问题、为什么要做、怎样才算完成，再设计实现。
- 执行任务不要背历史包袱。聚焦当前需求和验收标准，先查看全局影响面并设定计划，Review 通过后直接修改到位；不要害怕改动必要的核心文件，项目必须始终保持干净整洁。
- 以正式模块和正式调用链完成最小可用闭环；可以小步交付，但不能用临时入口、试验类或 demo 文档替代正式能力。
- 需要调整全局配置、启动入口、公共契约、目录结构或核心模块时，先 Review 影响范围，然后直接改到位。
- 代码和文档不得冗余。删除功能、入口、配置、依赖或文档条目时，必须检查调用关系和残留引用，并清理干净。
- 不堆兼容层、双路径、废弃字段、保留旧实现或未被需求支撑的适配逻辑。
- 项目技术栈固定为 Spring Boot 3、SQLite、MyBatis Plus 和 `hutool-all`；不得随意引入替代框架或重复能力库。

## 任务流程

执行代码、配置或文档改动前，按以下流程推进：

1. **CodeGraph 理解上下文**
   - 先用 `codegraph` 定位相关符号、调用关系、影响范围和模块边界。
   - 常用命令包括 `codegraph query`、`codegraph callers`、`codegraph callees`、`codegraph impact`、`codegraph affected`、`codegraph files`、`codegraph status`。
   - 如果本地还没有索引，使用 `codegraph init -i` 初始化。
2. **精确补充检索**
   - 在 CodeGraph 收敛范围后，再用 `rg`、`rg --files` 补充检索关键词、配置项、测试文件和文档引用。
   - 不大量横向扫描无关文件，不凭感觉改动未知模块。
3. **计划 Review**
   - 编码前给出简明计划，说明目标、涉及模块、设计方案、验证方式和主要风险。
   - 计划必须先回答“做什么、为什么做、怎样算完成”，再讨论实现细节。
   - 如果执行中发现计划不适用，先说明变化点并更新方案。
4. **实施与验证**
   - 只改当前任务必要文件，不做无关重构。
   - 删除或迁移旧代码时，用 CodeGraph 和 `rg` 检查调用、配置、测试、文档和资源残留。
   - 运行与改动范围匹配的测试、构建或检查。
5. **同步上下文**
   - 改动后同步相关文档、注释、接口契约或示例。
   - 完成后运行 `codegraph sync`；如果同步失败，先查看 `codegraph status` 和对应 `help` 信息再处理。

## 文档与目录

- 修改 README、目录树、推荐阅读顺序、文档索引、链接清单或交叉引用前，必须用 `rg --files` 或等价命令核对真实文件。
- 文档不得保留不存在的路径、旧链接、占位索引或超过项目现状的描述。
- 需要记录后续工作但文件尚未落地时，在当前文档中写清任务，不提前创建空链接或虚假索引。

## 架构边界

- 保持 `controller`、`agent`、`agent.runtime`、`service.spi`、`service.impl`、`domain`、`dto`、`dao`、`config` 的职责分离；新增目录必须对应明确职责。
- Controller 只承接协议适配和请求响应，不堆业务流程。
- Agent 负责智能体编排、模型交互和运行时协作，不承载通用业务服务。
- Service SPI 定义业务契约，Service Impl 承接业务编排和事务边界。
- Domain 表达核心业务实体和值语义，DTO 表达接口入参出参，DAO 只负责持久化访问。
- 不新增万能 `manager`、`helper`、`util`，不长期传递万能 Map。
- 如果职责归属不清，先收敛边界，再写代码。

## 设计与扩展

- 围绕目标、数据流、模块边界、失败场景和验收标准设计实现。
- 稳定变化点才抽象；一次性流程保持直白，不为了“看起来可扩展”制造层级。
- 当同一能力明确存在多种实现方式时，优先使用接口、策略、工厂、注册表或工具契约承载变化点，调用方依赖抽象。
- 目录结构应服务于职责分离和后续演进，不为单个类过早拆包，也不把不同职责塞进同一模块。
- 代码路径应清晰单一，避免双路径、兜底分支、废弃字段和未被需求支撑的适配层。

## 代码表达

- 类、方法和参数命名要表达“做什么、什么时候用、怎么用”。
- 新增 Controller、Service 接口、DTO、领域实体或扩展点时，补充必要 JavaDoc，说明使用时机、关键参数、可空规则、状态流转和返回语义。
- 接口说明写清扩展者必须遵守的契约；实现类说明写清适用场景和策略差异。
- 注释服务于契约理解和医学安全追溯，不重复代码字面含义，不用空泛注释掩盖命名或边界问题。
- 新增 Java 数据承载类、DTO、实体类时，统一使用 Lombok 管理 getter、setter、toString、equals/hashCode 等样板代码。
- 参数名必须体现业务含义，不使用含糊对象承载关键语义。

## 医学安全

- 涉及医学问答时必须保留证据引用、风险提示和可追溯信息。
- 不为了回答流畅性删除来源、禁忌、适应证、风险提示或审计信息。
