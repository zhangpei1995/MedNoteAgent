/**
 * Minimal Agent orchestration layer.
 *
 * <p>Classes in this package coordinate one user request and should avoid embedding
 * domain-specific step logic. Extend this package through clear orchestration boundaries
 * when adding real capabilities.</p>
 */
package org.med.note.agent;
